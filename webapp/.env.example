VITE_API_URL=http://localhost:3000
# Set only when OAuth must use a public callback origin different from the normal API.
# VITE_OAUTH_API_URL=https://api.example.com
# These values are intentionally public in the rendered legal documents. Keep real
# values only in the deployment environment; do not commit them to this repository.
# Set LEGAL_DOCUMENTS_CONFIGURED=true for a deployable production build.
VITE_PUBLIC_LEGAL_OPERATOR_NAME=
VITE_PUBLIC_LEGAL_OPERATOR_RECIPIENT=
VITE_PUBLIC_LEGAL_OPERATOR_ADDRESS=
